// import { getPersonalUserInfo } from "@/lib/user/user";
// import { validateUser } from "@/middlewares/auth";

// const someRouter = s.router(apiContract.something, {
//     someField: {
//         middleware: [validateUser], // for protected routes, replace this with [protectRoute.someRole]
//         handler: async ({ req, res }) => {
//             if (res.locals.user) {
//                 const userinfo = await getPersonalUserInfo({ username: res.locals.user.username });
//             }
//         },
//     },
// };
